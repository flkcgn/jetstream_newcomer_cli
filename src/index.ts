#!/usr/bin/env node

/**
 * jetstream-newcomer-cli
 *
 * A command-line tool that listens to the Bluesky Jetstream
 * and highlights new, likely human user accounts.
 */

// Later this will contain:
// - Jetstream WebSocket connection
// - Profile checks (isNew, isHuman)
// - CLI flags (e.g. --json, --limit, etc.)

console.log("jetstream-newcomer-cli says hello 👋");